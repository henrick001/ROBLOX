local gameIDToUrl = {
    [16732694052] = "https://raw.githubusercontent.com/henrick001/ROBLOX/refs/heads/main/Games/Fisch%20loader",  -- Fisch
}

-- Function to get the URL for the current game
local function GetGameScriptUrl()
    local currentGameId = game.GameId
    local url = gameIDToUrl[currentGameId]

    if url then
        return url
    else
        return nil
    end
end
